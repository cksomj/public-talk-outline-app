# Public Talk Outline App

공개강연 골자 완전판 암기 앱입니다.

## Local Run

```powershell
python -m http.server 5201 --bind 127.0.0.1
```

Then open:

```text
http://localhost:5201/
```

The app is a single static `index.html` file, so it can also be published with GitHub Pages.
